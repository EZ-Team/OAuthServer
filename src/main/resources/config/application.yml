server:
  port: 9999
security:
  oauth2:
    client:
      client-id: acme
      client-secret: acmesecret
      scope: read,write
      auto-approve-scopes: '.*'
spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: oauth-server